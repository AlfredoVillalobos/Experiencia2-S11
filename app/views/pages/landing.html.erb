<h1>Pages#landing</h1>
<p>Find me in app/views/pages/landing.html.erb</p>
<table>
    <tbody>
        <% @students.each do |student| %>
        <tr>
            <td><%= student.name %></td>
            <td><%= student.email %></td>
        </tr>
        <% end %>
    </tbody>
</table>
